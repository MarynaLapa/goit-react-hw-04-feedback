import { NotificationMessage } from "./styled/global";

const Notification = ({ message }) => {
    return <NotificationMessage>
        {message}
    </NotificationMessage>
};


export default Notification;

